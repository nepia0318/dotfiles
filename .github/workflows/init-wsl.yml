name: lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  translations:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Execute init.sh
      run: ${GITHUB_WORKSPACE}/init.sh
